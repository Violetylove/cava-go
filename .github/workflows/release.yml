# Builds and attaches cava-go binaries for windows/amd64 and linux/amd64
# to a GitHub Release on every version tag (v*).
#
# - `test`: verifies vet + unit tests natively on both platforms
#   (unit tests must run on their own OS; no cgo anywhere).
# - `release`: creates the GitHub Release (go-release-action only uploads
#   assets, it does not create releases).
# - `build`: matrix over [windows/amd64, linux/amd64] on ubuntu runners
#   (go-release-action is a docker action and only runs on Linux) using
#   wangyoucao577/go-release-action, which cross-compiles with
#   CGO_ENABLED=0 and uploads the packaged binaries as release assets.
name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Verify
        run: |
          go vet ./...
          go test ./...

  release:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true

  build:
    needs: [test, release]
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: windows
            goarch: amd64
          - goos: linux
            goarch: amd64
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build and upload
        uses: wangyoucao577/go-release-action@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          goos: ${{ matrix.goos }}
          goarch: ${{ matrix.goarch }}
          goversion: "1.26"
          project_path: ./cmd/cava
          binary_name: cava
          ldflags: "-s -w -X main.version=${{ github.ref_name }}"
          md5sum: false
          sha256sum: true
