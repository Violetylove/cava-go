// Command cava-go is a Windows console audio visualizer (cava clone).
// It captures system audio output (WASAPI loopback) and renders
// spectrum/waveform visualizations to the terminal.
package main

import "fmt"

func main() {
	fmt.Println("cava-go: project skeleton (M0). Audio capture pipeline comes in M1.")
}
